import Head from "next/head";
import { useState } from "react";


const Styleguide = (props) => {
   
    return (
        <div>
            I am a Styleguide
        </div>
    );
};

export default Styleguide;
